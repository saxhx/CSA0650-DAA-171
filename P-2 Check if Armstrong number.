#include<stdio.h>
int main(){
	int sum=0,r,n,temp;
	printf("enter the number: ");
	scanf("%d",&n);
	temp=n;
	printf("Salvin Sabu - 192210171\n");
	while(n>0)
	{
		r=n%10;
		sum=sum+(r*r*r);
		n=n/10;
	}
	if(temp==sum){
		printf("armstrong");
	}
	else{
		printf("not armstrong");
	}
	return 0;
}
